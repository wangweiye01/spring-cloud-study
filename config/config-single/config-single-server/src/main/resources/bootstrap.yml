spring:
  application:
    name: config-single-server
  cloud:
     config:
        server:
          git:
            uri: https://github.com/wangweiye01/config-demo
            username: username
            password: password
            default-label: master
            search-paths: config